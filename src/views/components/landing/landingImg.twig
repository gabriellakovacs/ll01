{# CHOOSE RANDOM IMAGE #}
{% set randomImg = random(images) %}

<!-- <img src="{{ randomImg.url }}" alt="{{ randomImg.title }}" class="landing-img js-landing-img">
<img src="{{ randomImg.url }}" alt="{{ randomImg.title }}" class="landing-img landing-img-hover js-landing-img"> -->

{% include "views/macros/_img-resize_landing.twig" with {
    'img' : randomImg,
    'classList': 'landing-img js-landing-img hide-while-loading'
} %}

{% include "views/macros/_img-resize_landing.twig" with {
    'img' : randomImg,
    'classList': 'landing-img landing-img-hover js-landing-img hide-while-loading'
} %}
